// examples/demo.ltl — runnable demo for ltl_dns_server

import std::io
use ltl_dns_server as lib

fn main() {
    io::println("ltl_dns_server demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}